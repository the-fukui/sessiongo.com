openapi: 3.0.0
info:
  title: SessionGO API
  version: 0.1.0
paths:
  /users:
    $ref: './users.yml'
  /events:
    $ref: './events.yml'
  /events/{id}:
    $ref: './events_id.yml'
  /users/{id}/events:
    $ref: './users_id_events.yml'
