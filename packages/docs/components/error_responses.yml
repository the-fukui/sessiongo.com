BadRequest:
  description: リクエストが無効です。
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yml#/ErrorResponse'
Unauthorized:
  description: このリソースへのアクセスが許可されていません。
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yml#/ErrorResponse'
InternalServerError:
  description: サーバー内部エラーが発生しました。
  content:
    application/json:
      schema:
        $ref: '../components/schemas.yml#/ErrorResponse'
